import 'package:flutter/material.dart';

class HomePagePrimaryText extends StatelessWidget {
  const HomePagePrimaryText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'You have pushed the button this many times:',
    );
  }
}
